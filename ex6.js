function removeDups(object) {
  for (let i = 0; i < object.length; i++) {
    let str = [];
    if (!object.includes()) {
      str += object[i];
    }
    console.log(str);
  }
}
removeDups([1, 0, 1, 0]);
