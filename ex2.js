function highestDigit(num) {
  for (let i = 0; i < num.toString().length; i++) {
    let max = 0;
    if (!num.toString().includes()) {
      max = num;
    }
    console.log(max);
  }
}

highestDigit(379);
highestDigit(2);
highestDigit(37);
